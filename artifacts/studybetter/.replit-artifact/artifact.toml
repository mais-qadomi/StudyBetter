kind = "web"
previewPath = "/"
title = "StudyBetter"
version = "1.0.0"
id = "artifacts/studybetter"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19942

[services.development]
run = "pnpm --filter @workspace/studybetter run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/studybetter", "run", "build" ]
publicDir = "artifacts/studybetter/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19942"
BASE_PATH = "/"
